<div dir="rtl">

# سجلُّ التغييرات

جميعُ التغييراتِ الجديرةِ بالذكرِ في هذا المشروع.
يتبعُ المشروعُ ترقيمَ [Semantic Versioning](https://semver.org/lang/ar/).

**صيغةُ الترقيم:** السلسلةُ سنةٌ‑شهرٌ تُكتَبُ semver بلا صفرٍ بادئ — سلسلةُ 26.09
رقمُها `26.9.0` لا `26.09.0` — و**كلُّ ما يسبقُ الاستقرارَ لاحقةٌ على الرقمِ نفسِه**:
`26.9.0-beta.1` ثمّ `26.9.0-beta.2` وهكذا حتّى `26.9.0` المستقرّ. فالسلسلةُ لا
يتغيّرُ رقمُها بينَ التجارب، وهو الترقيمُ نفسُه الذي تحملُه نسخةُ الهاتف. وقد
نُشِرَ أوّلُ إصدارَين بوسمَي `v26.9.0` و`v26.9.1` قبلَ التوحيد، وهما beta.1 وbeta.2.

---

## [26.9.0-beta.3] — غيرُ منشورٍ بعد

### أُضيف

- **بطاقةُ AppStream في الحزمة.** كانَ مركزُ البرامجِ يعرضُ «Unknown author» و
  «Licenses: Unknown» مهما ضُبِطت حقولُ الحزمة — فحزمةُ deb لا حقلَ رخصةٍ فيها
  أصلاً، والمطوّرُ يُقرَأُ من بطاقةِ AppStream لا من حقلِ Maintainer. فصارَ يُثبَّتُ
  في `/usr/share/metainfo/` ملفٌّ فيه الرخصةُ (‏GPL-3.0-or-later) والمطوّرُ
  والوصفُ والمزايا بالعربيّةِ والإنجليزيّةِ وروابطُ المشروع، ويُحدَّثُ مخزنُ
  AppStream بعدَ التثبيتِ فلا ينتظرُ المستخدمُ دورةً يوميّةً ليظهرَ ما فيه.
  ومعه `--license` و`--vendor` و`--url` في حقولِ الحزمةِ نفسِها — والرخصةُ حقلٌ
  حقيقيٌّ في rpm وإن لم تكن في deb.

  والرقمُ فيها **مولَّدٌ من `package.json`** بـ`build-scripts/gen-metainfo.js`
  ضمنَ `npm run build`، فلا يُكتَبُ رقمٌ في موضعَين. و`launchable` يُشيرُ إلى
  `gmd-gui.desktop` — وهو ما يُثبِّتُه electron-builder فعلاً من حقلِ `name` لا من
  `productName` — وبغيرِه تبقى البطاقةُ معلَّقةً لا تُنسَبُ إلى التطبيق.

### تغيَّر

- **نواةُ التنفيذِ صارت تحملُ مهامَّ متوازية.** كانت في العمليّةِ الرئيسةِ مقبضُ
  عمليّةٍ واحدٌ وعَلَمُ إلغاءٍ واحدٌ على مستوى الوحدة، فطلبٌ ثانٍ أثناءَ الأوّلِ يدهسُه:
  يُصفِّرُ إلغاءَه، ويسرقُ مقبضَه، فيقتلُ زرُّ الإلغاءِ آخرَ ما بدأَ لا ما قصدَه
  المستخدم. والأحداثُ لا تحملُ إلّا العنوانَ فلا يُميَّزُ خرجُ مهمّةٍ من أختِها — وفي
  المُصيِّرِ مرجعا معالجةٍ مفردانِ يكتبُ فوقَهما كلُّ استدعاء.

  صارَ لكلِّ مهمّةٍ **معرِّفٌ** يُولِّدُه المُصيِّرُ قبلَ الطلبِ — لأنّ الخرجَ يبدأُ
  بالوصولِ قبلَ أن يعودَ ردُّ النداء — وخريطةٌ في العمليّةِ الرئيسةِ فيها مقبضُها
  وعَلَمُها، وخريطةُ معالِجاتٍ في المُصيِّر. و`cancel-command` يأخذُ المعرِّفَ فيُلغي
  مهمّةً بعينِها وبلا معرِّفٍ يُلغي الكلَّ، ومعرِّفٌ مستعمَلٌ يُرَدُّ بدلَ أن يدهسَ
  صاحبَه، وحوارُ الإغلاقِ يَعُدُّ المهامَّ الجارية.

  **ولا أثرَ لهذا في الواجهةِ بعدُ**: هي ما تزالُ تُشغِّلُ مهمّةً واحدة. وهذا أساسٌ
  لِما يليه — قسمانِ مستقلّانِ يعملانِ معاً كما صارَ في نسخةِ الهاتف.

  وأربعةُ فحوصٍ جديدةٍ تحرسُه: مهمّتانِ معاً بلا اختلاطِ خرج، ومعرِّفٌ في كلِّ ردٍّ،
  وإلغاءُ إحداهما لا يمسُّ الأخرى، ومعرِّفٌ مكرَّرٌ مرفوض.

### أُصلح

- **عناوينُ الأقسامِ كانت تُحاذي اليسارَ في العربيّة** فتبتعدُ عن أيقونتِها وتلتصقُ
  بسهمِ الطيِّ في الطرفِ الآخَر. والسببُ `text-left` في صفٍّ عنوانُه `flex-1`: يُلصِقُ
  النصَّ بالحافّةِ اليسرى من حاويةٍ تملأُ العرضَ كلَّه، فلا ينفعُ انعكاسُ الصفِّ في
  وضعِ اليمين. صارَ `text-start` المنطقيَّ في الإعداداتِ وخياراتِ التنزيلِ المباشرِ
  وقوائمِ التشغيل — والعنوانُ يُحاذي أيقونتَه: يميناً في العربيّةِ ويساراً في
  الإنجليزيّة. ومثلُه `mr-2` بينَ أيقونةٍ ونصٍّ صارَ `me-2`.

---

## [26.9.0-beta.2] — 2026-09-04

> نُشِرَ وسمُه `v26.9.1` قبلَ توحيدِ الترقيم؛ وهو beta.2 من سلسلةِ 26.09.

نقلُ ما استُحدِثَ في نسخةِ الهاتفِ إلى الحاسوب، مع احترامِ خصوصيّةِ كلٍّ منهما.

### أُضيف

- **سجلُّ التنزيلات.** كلُّ رابطٍ جُرِّبَ يبقى — نجحَ أو فشل — بمعلوماتِه وسببِ فشلِه
  كاملاً، مع مُرشِّحٍ ثلاثيٍّ وحذفٍ فرديٍّ ومنتقىً وجماعيّ، وإعادةِ محاولةٍ تملأُ
  الرابطَ وتفتحُ شاشتَه. وكان إغلاقُ صندوقِ النتيجةِ يُضيعُ الرابطَ وسببَ الفشلِ معاً
  فلا يبقى ما تُعادُ به الكرّة. والعمليّاتُ المحلّيّةُ — تحويلُ ملفٍّ وقصٌّ ومعلوماتٌ —
  لا تُسجَّل: السجلُّ للروابطِ لا لكلِّ أمرٍ يُنفَّذ.
- **اقتصاصُ جزءٍ عندَ التنزيل.** شاشةُ «قصّ فيديو/صوت» تقصُّ ملفّاً منزَّلاً؛ وهذا
  يقصُّ قبلَ التنزيلِ فلا يُنزَّلُ ما لا يُراد، والفرقُ في الفاتورةِ لا في النتيجة.
  ويرفضُ بعضُ المواقعِ الطلبَ الجزئيَّ — يوتيوب منها يردُّ 403 — فتبقى شاشةُ القصِّ
  طريقاً ثانياً، وهو مذكورٌ في تنبيهِ الحقلَين.
- **مجلَّدٌ لكلِّ قائمةِ تشغيل.** كانت مقاطعُ القائمةِ تُرمى في المجلَّدِ المختارِ
  مختلطةً بغيرِها؛ صارت في مجلَّدٍ باسمِها، ويكتبُ yt-dlp الاسمَ بنفسِه فلا يُبنى
  نصّاً هنا. ومعه `--ignore-errors` فعنصرٌ محجوبٌ لا يُهدِرُ ما نزلَ قبلَه.

### أُصلح

- **المحدِّثُ الذاتيُّ كان يُهمِلُ لاحقةَ ما قبلَ الإصدار**، فيقرأُ `26.9.1-beta.1`
  و`26.9.1-alpha.1` رقماً واحداً. والأثرُ أنّ إصداراً تجريبيّاً لا يُرى أبداً ما دامَ
  رقمُه رقمَ المستقرّ. صارت المقارنةُ بترتيبِ semver: المستقرُّ يسبقُ ما قبلَه عندَ
  تساوي الرقم، والمعرّفُ الرقميُّ يُقارَنُ عدداً لا حرفاً فـ`beta.10` بعدَ `beta.9`.
  وتُحُقِّقَ من ثمانِ حالاتٍ حدّيّة.
- **ملاحظاتُ الإصدارِ كانت تُعرَضُ ترميزاً خاماً** داخلَ `<pre>`؛ صارَ العريضُ عريضاً
  والشيفرةُ بخطٍّ ثابتٍ والقوائمُ بنقاط.
- **إغلاقُ النافذةِ أثناءَ تنزيلٍ جارٍ كان يقتلُه صامتاً** وما نزلَ يُهمَل. صارَ
  يُسألُ أوّلاً — وخلافاً لنسخةِ الهاتفِ لا خدمةَ تُكمِلُ هنا، فالسؤالُ هو كلُّ ما
  يمنعُ الضياع.

---

## [26.9.0-beta.1] — 2026-09-02

> نُشِرَ وسمُه `v26.9.0` قبلَ توحيدِ الترقيم؛ وهو beta.1 من سلسلةِ 26.09.

إصدارُ سلسلةِ 26.09: يعالجُ ثلاثةَ أعطابٍ أمنيّةٍ ووظيفيّةٍ في نواةِ تنفيذِ الأوامر،
ويضيفُ التحديثَ الذاتيّ.

> **صيغةُ الترقيم:** السلسلةُ سنةٌ‑شهر، وتُكتَبُ semver بلا صفرٍ بادئ — سلسلةُ 26.09
> رقمُها `26.9.0` لا `26.09.0`، لأنّ electron-builder يحذفُ الصفرَ البادئَ من أسماءِ
> الحزمِ فيختلفُ المصدرُ عن الحزمِ عن الوسم، وهو ما حدثَ في 26.05 فعلاً.
> ونسخةُ الهاتفِ تشاركُ السلسلةَ نفسَها، فأوّلُ إصدارٍ لها 26.9.0 كذلك.

### أمن

- **إزالةُ ثغرةِ حقنِ أوامرِ الصدفة.** كانت كلُّ عمليّةٍ تُبنى نصّاً ثمّ تُنفَّذُ بـ
  `spawn('bash', ['-c', cmd])`، والرابطُ أو مسارُ الملفِّ يُوضَعُ بين علامتَي اقتباسٍ مزدوجتَين.
  لكنّ الصدفةَ تُوسِّعُ `$(...)` والعلاماتِ الخلفيّةَ داخلَ الاقتباسِ المزدوج، فرابطٌ مثلُ
  `https://example.com/$(...)` كان يُنفِّذُ محتواه بصلاحيّاتِ المستخدم.
  صارت الأوامرُ كلُّها تُمرَّرُ الآن مصفوفةَ وُسَطاءَ إلى `spawn(bin, argv)` بلا صدفةٍ أصلاً،
  فالرابطُ والمسارُ بياناتٌ لا شيفرة. شمِلَ التحويلُ ستّةَ مكوّناتٍ و`ffprobe` و`yt-dlp --dump-json`
  و`--flat-playlist` و`which` وتثبيتَ الحزم.
- **تقييدُ بروتوكول `media://`.** كان يخدمُ أيَّ مسارٍ في نظامِ الملفّاتِ بلا قيد.
  صار مقصوراً على الملفّاتِ التي اختارَها المستخدمُ فعلاً عبرَ نوافذِ البرنامج.
- **قصرُ تثبيتِ الحزمِ على أدواتٍ معروفة** (`wget`, `aria2c`, `ffmpeg`) بدلَ تمريرِ الاسمِ كما ورد.

### إصلاحات

- **تراكمُ مستمعي IPC.** كان `handleRunCommand` يسجّلُ مستمعَي الخرجِ والانتهاءِ في كلِّ عمليّةٍ
  ويُرجِعُ دالّةَ تنظيفٍ لا يستدعيها أحد. النتيجةُ أنّ العمليّةَ رقمَ N كانت تُعالِجُ كلَّ سطرِ
  خرجٍ N مرّةً وتفتحُ N صندوقَ نتيجة، ويضطربُ شريطُ التقدُّمِ لتضاعُفِ النصِّ المتراكم.
  صارت المستمعاتُ تُسجَّلُ مرّةً واحدةً لعمرِ التطبيق.
- **الحكمُ الخاطئُ بالفشلِ على عمليّاتٍ ناجحة.** كان النجاحُ يُقاسُ بـ
  `!output.match(/error|ERROR|failed|فشل/i)`، و`yt-dlp` يطبعُ `ERROR` عندَ محاولةٍ فاشلةٍ
  ثمّ ينجحُ بالتالية، و`ffmpeg` يطبعُ تحذيراتٍ تحوي الكلمة. صارَ الحكمُ برمزِ الخروجِ
  وحدَه، يُضافُ إليه التحقُّقُ من وجودِ الملفِّ الناتجِ فعلاً في عمليّاتِ التحويلِ والقصّ.
- **إلغاءُ العمليّةِ يُوقِفُ المهامَّ المنتظِرة** أيضاً، لا المهمّةَ الجاريةَ وحدَها.
- **حالةُ فشلِ التشغيل.** أمرٌ غيرُ موجودٍ (`ffmpeg` غيرُ مثبَّتٍ مثلاً) كان يمرُّ صامتاً؛
  صارَ يُبلَّغُ عنه في الخرجِ ويُعَدُّ فشلاً.
- **الجودةُ صارت سقفاً على الضلعِ الأصغرِ لا على الارتفاع.** كان الترشيحُ
  `bv*[height<=720]+ba/best[height<=720]`، وهو صحيحٌ في الفيديو الأفقيِّ حيثُ الارتفاعُ هو
  الضلعُ الأصغر، وخاطئٌ في العموديِّ حيثُ ينقلبُ الأمر: مقطعُ «720p» عموديّاً مقاسُه 720×1280،
  فارتفاعُه 1280 يتجاوزُ كلَّ سقفٍ يطلبُه المستخدم — حتّى سقفَ 1080 — فلا تُطابِقُ الشرطَ
  صيغةٌ واحدةٌ ويموتُ التنزيلُ بـ`Requested format is not available`. لم يُبلَّغ عن العطبِ في
  نسخةِ الحاسوبِ قطُّ لأنّ التجاربَ جرت على مقاطعَ أفقيّة، وقد تحقّقنا أنّه يقعُ فيها كذلك
  بأحدثِ نسخةٍ من yt-dlp. صارَ الترشيحُ `-S res:N`، وحقلُ `res` عندَ yt-dlp هو الضلعُ الأصغرُ
  نفسُه فيَصحُّ في الاتّجاهَين، وهو ترتيبٌ لا شرطٌ قاطع: يُقدِّمُ الأقربَ عندَ السقفِ أو دونَه،
  فإن لم يكن إلّا ما فوقَه اختارَ أصغرَ ما فوقَه بدلَ أن يفشل. شمِلَ الإصلاحُ شاشتَي التنزيلِ
  والتنزيلِ مع التحويل — وقد وُحِّدت قائمتاهما في `src/quality.js` بعدَ أن كانتا نسختَين
  متطابقتَين — وكذلك `gmd` و`gmd-gui`.

### ميزة جديدة

- **التحديثُ الذاتيّ.** صارَ GMD يستقبلُ التحديثاتِ عندَ صدورِها ويُنزّلُها
  ويثبّتُها دونَ أن يغادرَ المستخدمُ البرنامج:
  - تحقُّقٌ صامتٌ عندَ التشغيل، مُقيَّدٌ بمرّةٍ كلَّ ستّ ساعات، مع شريطِ تنبيهٍ
    في أعلى النافذة. يمكنُ تعطيلُه من الإعدادات.
  - تنزيلٌ **قابلٌ للاستئناف** عبرَ ترويسةِ `Range`، فانقطاعُ الاتّصالِ في
    منتصفِ حزمةٍ حجمُها مئةُ ميغابايتٍ لا يُلزِمُ البدءَ من الصفر، ويُتحقَّقُ
    من الحجمِ قبلَ قبولِ الملفّ.
  - تثبيتٌ يسلكُ المسارَ الذي يناسبُ طريقةَ تثبيتِ النسخةِ العاملة: نسخةُ
    AppImage تستبدلُ نفسَها، وحزمُ deb/rpm تُسلَّمُ لمديرِ حزمِ النظامِ عبرَ
    `pkexec`، وشجرةُ التطويرِ تقولُ ذلك صراحةً بدلَ أن تتظاهر.
  - خيارُ قبولِ الإصداراتِ التجريبيّة، معطَّلٌ افتراضيّاً.

  اختُبِرَ المسارُ كاملاً على الحزمةِ المنشورةِ الحقيقيّة: تنزيلٌ، فقطعٌ للاتّصالِ في
  منتصفِه، فاستئنافٌ من الملفِّ الجزئيّ، فتحقُّقٌ من الحجم، فاستبدالٌ ذرّيٌّ للحزمة.
  وكشفَ هذا الاختبارُ عطباً كانَ سيظهرُ للمستخدم: `req.destroy()` يُطلِقُ `close`
  ولا يُطلِقُ `error`، فكانَ وعدُ التنزيلِ لا يُحسَمُ أبداً بعدَ الإلغاء وتبقى نافذةُ
  التقدُّمِ مفتوحةً إلى الأبد.

### تحسينات

- **التحويلُ الدُّفعيُّ صارَ طابوراً حقيقيّاً.** كانت الملفّاتُ تُوصَلُ بـ `&&` في أمرٍ واحدٍ
  فيسقطُ الباقي كلُّه عندَ فشلِ ملفٍّ واحد. صارَ كلُّ ملفٍّ عمليّةً مستقلّة، ويعرضُ صندوقُ
  التقدُّمِ عدّادَ `المهمّة / المجموع`.
- **رقمُ الإصدارِ في مكانٍ واحد.** كان مكرَّراً في `package.json` و`main.js` و`index.html`
  وملفَّي الترجمة، وبثلاثِ صيغٍ متضاربة (`26.05.0` في المصدر، `26.5.0` في أسماءِ الحزم،
  `GMD-26.05` في الوسم). صارَ `package.json` المرجعَ الوحيدَ عبرَ `app.getVersion()`،
  والصيغةُ semver صحيحةً فلا يُعيدُ أحدٌ تطبيعَها. والاختباراتُ تقرأُ الرقمَ منه أيضاً،
  فرفعُ الإصدارِ لم يعُد يستلزمُ تعديلَها.

### تغييراتٌ داخليّة

- عقدُ `run-command` تبدَّل: يستقبلُ الآن `{ jobs: [{ bin, args, outFile? }], title }`
  بدلَ `{ cmd, title }`، وينفّذُ المهامَّ تِباعاً.
- حدثُ `command-output` يحملُ `jobIndex` و`jobCount`.

---

## [26.05.0] — 2026-04-30

إعادةُ البناءِ الكاملةِ بواجهةٍ رسوميّةٍ حديثة.

- واجهةُ **Electron 28 + React 18 + Vite 5 + Tailwind CSS 3** بدلَ Zenity.
- تسعُ شاشات: تنزيلُ فيديو · تنزيلُ صوت · تنزيلٌ وتحويل · تحويلُ ملفّاتٍ محلّيّة ·
  خياراتٌ إضافيّة · قصُّ وسائط · معلوماتُ الوسائط · الإعدادات.
- دعمٌ كاملٌ للعربيّةِ والإنجليزيّةِ مع RTL.
- مساراتُ حفظٍ افتراضيّةٌ لكلِّ نوعِ ملفّ.
- معاينةُ الفيديو والصوتِ داخلَ البرنامجِ مع شريطِ قصٍّ تفاعليّ.
- تحويلٌ دُفعيٌّ ودعمُ صيغِ الصور.
- خياراتُ ترميزٍ متقدّمةٌ لكلِّ حاوية.
- كشفُ مديرِ الحزمِ وتثبيتُ الاعتماديّاتِ عبرَ `pkexec`.
- تنزيلٌ مباشرٌ بـ `wget` و`aria2c`.
- حزمُ AppImage و deb و rpm.

---

## [1.92] — 2026-02-27

آخرُ إصدارٍ من نسخةِ الطرفيّةِ والـ Zenity. راجِع `gmd` و`gmd-gui` في جذرِ المستودع.

</div>
